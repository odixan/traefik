# Traefik Configuration
TRAEFIK_DASHBOARD_CREDENTIALS=admin:$$2y$$10$$example_hashed_password

# Environment Configuration
# ENVIRONMENT=development  # Options: development, production
# TZ=Europe/Madrid

# Production Domain Configuration (for Let's Encrypt)
# DOMAIN=yourdomain.com
# EMAIL=your-email@yourdomain.com

# Example credentials generation:
# echo $(htpasswd -nb admin your_password) | sed -e s/\\$/\\$\\$/g
# Remember to double the $ signs in docker-compose environment

# GitOps Notes:
# - For local development: Use self-signed certificates (default)
# - For production: Uncomment production labels in docker-compose.yml
# - Update DOMAIN and EMAIL variables for Let's Encrypt in production
